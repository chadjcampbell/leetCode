function objDiff(obj1: any, obj2: any): any {
  let newObj = {};
  //Only compare common keys

  //If both primitave and diff, then diff

  //If one is obj and other isn't, then diff

  //If one is array and one is obj, then diff

  //If both arr, or both obj, use recursion

  return newObj;
}
